# Archived Verilator entry points

Files here are retained for provenance only. They are not supported build paths.

`verilate.sh` predates the current RTL layout and references removed modules. Use
the targets in `verilator/Makefile` for maintained Verilator builds.
